# Enforce the Node version declared in package.json engines
engine-strict=true

# Install peer dependencies automatically (pnpm 9 default, made explicit)
auto-install-peers=true

# Do not fail install on peer-dependency version mismatches from transitive deps
strict-peer-dependencies=false
